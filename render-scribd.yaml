services:
  - type: web
    name: netcookies-scribd-checker
    runtime: docker
    dockerfilePath: ./Dockerfile.scribd
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"
